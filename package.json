{
  "name": "gitpost",
  "private": true,
  "scripts": {
    "build": "pnpm --dir web generate",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
